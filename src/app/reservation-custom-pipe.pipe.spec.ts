import { ReservationCustomPipePipe } from './reservation-custom-pipe.pipe';

describe('ReservationCustomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ReservationCustomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
